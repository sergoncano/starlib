# Starlib

Starlib is a rust library for grid-based TUI game development. It takes care of collisions, rendering layers, creating menus and titles, controlling the flow of events among entities in the game and also provides nice abstractions for certain other aspects of a game.

To get started, take a look at the [documentation](https://docs.rs/starlib/latest/starlib/).
